# tasks

## todo
- [x] inspect root and `zig/` contents for conflicts
- [x] move project files from `zig/` into repo base dir
- [x] verify root layout and note results

## review
- moved all top-level contents from `zig/` into `/Users/hzare/projects/zmenu`
- verified `build.zig`, `justfile`, `docs/`, `samples/`, `scripts/`, `src/`, and `zig-out/` now exist at the root
- confirmed `zig/` was removed after the move
